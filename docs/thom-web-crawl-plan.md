## Thom Bot — Website Crawl, Index & Product-Link Cleanup Plan (v2.1 — MERGED: per-site-verified, amended for wacarchitectural.com, the 4-main-brand model, and the aiSpire comparison verdict; per Davis 2026-07-20; objection-resolved: category-listing `?p=` traversal)

> **Merge notes (conflict resolution).** Amended A and Amended B were reconciled into this single v2. The two headline "conflicts" turned out to be convergences: **(1) aiSpire authority — both amendments independently set brand-corporate at 1.1**; adopted, because Δ=+0.1 places aiSpire strictly between the 1.0 marketing baseline and the 1.2 main-brand tier, exactly encoding Davis's "one level below the 4 mains" as a tiebreak (max bias +0.0004 at λ=0.004) without moving any other tier. **(2) Comparison-page policy — both amendments reached the identical full-denylist verdict** for `/comparison/` + `/abicuscomparison/`, grounded in the same live verification (single raster image naming real competitors, no separable safe text); adopted in full. Two genuine sub-conflicts were resolved as follows, with reasoning inline where they appear: **(a)** the authority-tier ladder uses A's explicit per-(domain,path) enumeration rather than B's generic path patterns (`/about`,`/company`,`/ourstory`…) — B's generic `/company` pattern would silently promote `aispire.com/company/` to 1.2 and contradict the 1.1 tier both amendments intend; explicit enumeration is the only shape that can't self-contradict (see D). **(b)** the rollout uses B's 9-step order (aiSpire enable split into its own step) rather than A's 8-step fold-into-"brand sites" — aiSpire's enable carries two verifications no other site has (the comparison-page denylist and the 1.1 tier), so it warrants its own gate (see I). Everything else is a superset merge: where one amendment carried extra verified detail (B's byte-size dedup evidence, WP page IDs, `brandSite()` call chain; A's E.2b subsection split, three-way T3 variant), it is retained.
>
> **v2.1 edit note (objection resolution).** Planner-A correctly identified an internal contradiction: the per-host `?p=` preservation exception (A) was scoped to /projects and /news only, while B's facet-drop safety argument and E.2b's ID-enumeration path both DEPEND on traversing `?p=N` pagination on base category listings (audit-verified: `/na/products/indoor/12` is page 1 of 7) — and E.2b's claim that the A-section exception covered this was false as written. Under the global bare-`p` strip, the v1-active wacarchitectural evidence harvest (E.2, both regions, report-only) would have silently seen only page 1 of each category. Fixed in this revision: the exception is extended to base (unfaceted) category listing paths — faceted 4th-segment variants stay dropped — with E.2b's cross-reference corrected, a dedicated frontier.test fixture pair (preserved-on-base / stripped-on-faceted), and category-pagination traversal added to rollout step 6's verification list.

**Thesis (unchanged).** Extend `apps/docs-ingest` — do NOT build a parallel crawler. Add a sitemap/robots-driven capture step and one new `source_system='web_crawl'`, reusing the status-as-checkpoint model, `htmlToText`, `chunkText`, `embed()`/`toVectorLiteral`, and `kb_documents`/`kb_chunks`/`kb_search`. New machinery only: (1) a persisted URL frontier (now including a **seeded-BFS discovery mode** for the one sitemap-less host), (2) a per-document **authority** score folded into `kb_search` as a **bounded, band-gated, intent-driven ADDITIVE bias** (not a multiplier — see D.3), (3) a PDP reconciliation pass healing `pdp_urls`+`product_documents` via a **reverse code→SKU index** and a **shared PDP-helpers module**. One migration: `0054_thom_web_crawl.sql`. **All 6 sites live-audited, `siteWorks=true`**; architecture holds, per-site details corrected below.

**Brand model (ratified by Davis, 2026-07-20 — governs brand tagging, authority tiers, and the crawl roster).** WAC Group = **four MAIN brands: WAC Architectural, WAC Lighting, Modern Forms, Schonbek.** **aiSpire is group-affiliated but sits one level BELOW the 4 main brands** — it is a full crawl/ingest target, but its brand-corporate content is tiered below main-brand-corporate (1.1 vs 1.2, see D). **Crawl roster is therefore SIX sites:** wacgroup.com (corporate), wacarchitectural.com, waclighting.com, modernforms.com, schonbek.com (mains), aispire.com (affiliated). This model is restated wherever brand roster, authority tiers, or brand tagging appear below.

### A. Crawl architecture

New `crawl/` modules (`frontier`,`robots`,`sitemap`,`extractPage`,`authority`,`reconcilePdp`) wired into `index.ts` as **Step W** behind `THOM_WEB_CRAWL=1`, allowlist `THOM_CRAWL_SITES`, same Node CLI in Actions. **Two-tier:** Step W = discover + fetch HTML + extract main text + hash **normalized text** + store raw HTML in R2 `web/{brand}/{sha}.html` + upsert `kb_documents` (status omitted → changed land `pending_extract`). Step B (`processDoc`, extended) = `web_crawl` branch reads R2 (re-fetch fallback) → extract → `chunkText` → `embed` → `kb_chunks` → `active`.

**`crawl_frontier` table** (0054): `url` PK canonical, `host`,`site`,`doc_type_guess`,`status`(discovered|fetched|skipped|error|superseded),`http_status`,`etag`,`last_modified`,`content_hash`,`depth`,`last_crawled_at`, + PDP evidence: `discovered_slug`,`discovered_spec_sheet_url`,`model_codes text[]`,`resolved_skus text[]`,`resolved_family`,`resolution_state`(unresolved|one_sku|family|collision),`published_at`.

**robots/sitemap.** Per host: parse robots for our UA + Crawl-delay, collect `Sitemap:` + `/sitemap.xml`, recurse indexes. **Audited:** wacgroup `Crawl-delay:10` applies to `*`/our UA → honor 10s; other sites' delays are scoped to named scraper bots, not us. **Rule: read each host's Crawl-delay at runtime, honor max(robots,default); never assume default.** aispire has **no `Sitemap:` directive → `/sitemap.xml` fallback is MANDATORY** (verify `robots.ts` always tries it). www→apex canonicalization required on **waclighting** (robots Sitemap=www, locs=apex) and **modernforms** (declared www redirects to non-www); strip `www.`. **Canonical-host normalization is per-host config, NOT a blanket www-strip:** **wacarchitectural.com is the opposite direction — apex 301s to www (confirmed), and the site emits no `<link rel=canonical>` anywhere to derive it from, so hardcode apex→www for this domain.**

**wacarchitectural.com — no robots, no sitemap, seeded-BFS discovery (audited).** Neither `/robots.txt` nor any sitemap exists: the Blazor catch-all route returns **HTTP 200 with `text/html` SPA-shell markup for EVERY unmatched path** (verified across `/robots.txt`, `/sitemap.xml`, `/sitemap_index.xml`, `/wp-sitemap.xml`, `/na/sitemap.xml`, `/int/sitemap.xml`, and nonsense paths). `robots.ts`/`sitemap.ts` MUST validate response shape (Content-Type text/plain or text/xml, body starting `User-agent`/`<?xml`) and treat an HTML-shaped 200 as **absent** (allow-all, no sitemap) — never attempt to parse the markup as robots/sitemap syntax. Discovery for this ONE host is a **new frontier mode: hardcoded seed URLs + pure link-following BFS as the PRIMARY mechanism** (not a fallback): seed `https://www.wacarchitectural.com/na` and `/int` plus their nav pages (`/na/products`, `/na/products/indoor`, `/na/products/outdoor`, `/na/projects`, `/na/news`, `/na/resources`, `/na/about`, `/na/contact` and `/int` equivalents). **Never seed from `/`** — root issues an IP-geolocation 302 to /na or /int and sets a `wac_location` cookie, so its outcome is non-deterministic per crawl-runner egress IP. No Crawl-delay exists → crawler default ~1 req/s.

**Frontier/dedup.** Canonicalize: lowercase host, drop fragment, strip `utm_*`,`s`,`p`,`paged`,`orderby`,`add-to-cart`,`filter_*`,`replytocom`,`brand`,sessions; collapse trailing slash; honor `<link rel=canonical>`. **Per-host exception (wacarchitectural): PRESERVE `?p=` during discovery traversal only, on THREE listing shapes — `/{region}/projects`, `/{region}/news`, AND the base (unfaceted) category listings `/{region}/products/{indoor|outdoor}/{categoryId}`** — pagination on all three is a real static `?p=N` href (projects: 4 pages, news: 7, categories audit-verified: `/na/products/indoor/12` is page 1 of 7), and the global bare-`p` strip would collapse `?p=2..N` into `?p=1` and silently orphan every detail link that only appears on later pages. The category-listing arm is **load-bearing for E.2/E.2b**: category `?p=N` pagination is the ONLY enumeration path for `/product-detail/{id}` IDs on this host (no sitemap, no search endpoint), and it also carries the category-card model-code evidence harvest — without it, the v1-active evidence collection sees only page 1 of each category. **The exception applies ONLY to the base 3-segment category path; faceted 4th-segment variants (`.../{categoryId}/{facet}`) stay dropped as facet-junk (B) and never inherit the `?p=` preservation** — a `?p=` on a faceted path is skipped along with the path itself. Only the traversal keeps the param; the listing pages themselves remain junk/skip (`web_category`), and discovered detail URLs are distinct and unaffected. **Soft-404 guard (wacarchitectural):** an invalid `/{region}/product-detail/{id}` returns **HTTP 200** with `<title>Not Found | WAC Architectural</title>` and a near-empty body ("The product you are looking for is not available in NA.") — classification MUST detect this via title-text match, not HTTP status, and treat it as superseded/skip, never ingest. Genuine junk slugs on this host do hard-404 normally, so standard 404 handling holds for non-detail paths.

**Politeness.** `mapPool` POOL=1/host, hosts concurrent, ~1 req/s + jitter, respect larger robots delay. **Transparent UA** `WAC-Marketing-App/1.0 (+thom web crawl; contact WAC IT)` — do NOT spoof a browser: waclighting Cloudflare Bot Mgmt hard-403s a Chrome-spoofing UA but serves 200s to honest UA. **wacarchitectural is plain nginx, NOT behind Cloudflare (no cf-ray)** — honest UA, spoofed UA, and bare curl all 200 identically; no bot_block risk expected on that host. Conditional GET → 304=skip.

**Static vs JS.** All confirmed server-rendered. **Confirmed static (dropped from JS-fallback list): wacgroup, waclighting, modernforms, aispire, and wacarchitectural** (the last is a **Blazor Web App** — server-rendered .NET with `blazor.web.js` enhanced navigation, NOT WordPress; full content in raw HTML). **Schonbek: content static, PDP body JS-injected** (`wp-json` `content.rendered` empty) → add schonbek PDPs to `too_thin`/`js_shell` watchlist (moot; PDP prose off by default). Empty shell → `last_error='js_shell'`,`failed`. Browser-Rendering fallback stays behind `THOM_WEB_JS_FALLBACK=1`, built only if audit shows material JS surface, and **must be WAC-IT-allowlisted before use on waclighting** (real headless fingerprint could trip the 403).

**Region handling (wacarchitectural only).** `/na/*` and `/int/*` are a genuine regional split, not a mirror: **content pages (about, news, resources, projects, project-detail, contact) carry IDENTICAL PROSE across regions but are NOT byte-identical** (region-prefixed nav hrefs, differing mega-menu category sets, region-specific new-product/recent-projects widgets — verified /na/about 31,252 B vs /int/about 31,610 B, zero prose diffs). Crawl **/na as canonical for content types, skip the /int equivalents as near-dups** (same per-host dedup precedent as wacgroup `/` vs `/about/`), and **implement the dup check on the EXTRACTED prose block, never raw page bytes** — a naive byte/hash diff never matches and defeats the dedup. **The product catalog genuinely DIFFERS per region** (verified: `/na/product-detail/968` soft-404s "not available in NA" while `/int/product-detail/968` renders the real VOLTA PDP) — so PDP evidence harvest (if/when enabled) must cover BOTH `/na/product-detail/*` and `/int/product-detail/*`; never cross-region-dedup product pages.

**Re-crawl.** Weekly cron in new `.github/workflows/site-crawl.yml` (separate concurrency) + dispatch. Change detection: sitemap `<lastmod>` → conditional-GET 304 → `content_hash`. **wacarchitectural has no sitemap/lastmod source ⇒ change detection is conditional-GET/`content_hash` only, over the BFS frontier.** 404/410/dropped-from-sitemap → `superseded`; wacarchitectural soft-404 stubs → superseded via the title-match rule above.

### B. Per-site taxonomy

`classify(host, path, sitemapFile)` — pure/unit-tested; `SLUG_RE`/`SKIP_SLUGS` imported from `@wac/shared` (E.0). **News classified by sitemap provenance where paths are flat** (modernforms `post-sitemap.xml`). **Common junk (all 6):** search/facets/pagination, cart/account, `wp-admin`/`wp-json`/feed, tag/author/date archives, non-HTML by extension, help-center hosts. **Junk for all sites:** corporate legal `/privacy-policy/`,`/terms-*`,`/eula`,`/contact-us/`, **plus bare `/contact$`** (wacarchitectural uses `/contact`, not `/contact-us`).

- **wacgroup.com** — corporate, content-only, **zero PDPs**. Single `page-sitemap.xml`, **11 URLs = complete inventory** (no BFS). **7 keep, all tier 1.5:** `/about/`,`/about/responsibility/`→`web_company`; `/technology/`,`/lightandhealth/`→`web_technology`; `/commercial/`,`/residential/`,`/custom/`→`web_capabilities`. **4 junk:** `/` (near-dup of `/about/`, per-host dedup), `/contact-us/`,`/terms-and-conditions/`,`/privacy-policy/`. **Drop `web_news`** (no native news; `/news/`,`/leadership/` 404). Expected **0 web_product rows** (document it).
- **wacarchitectural.com** — **NEW 6th target, full MAIN-brand treatment (Davis 2026-07-20); the architectural outlier of the six:** Blazor SSR on nginx, no WordPress/Yoast, no robots/sitemap (see A: seeded-BFS, canonical host = www, /na canonical for content, prose-level /int dedup). Taxonomy (all under `/na/...` primary): `/na` + `/na/about` → `web_company`, **tier 1.2** (verified genuine brand-corporate copy: "WAC Architectural is part of WAC Group… backed by the scale and resources of WAC Group" — squarely the same band as waclighting `/ourstory` and modernforms `/who-we-are`); `/na/news` + `/na/news-detail/{id}` → `web_news` @ 0.9 (`?s=newest|oldest`, `?t=1|2|3` are sort/filter params → strip as junk); `/na/projects` + `/na/project-detail/{id}` (case-study/portfolio pages; ids 90,92,102,116,117,122 seen) → **fold into `web_capabilities`, OPT-IN OFF BY DEFAULT** like `web_product` (no new doc_type per the plan's additive-not-proliferating bias; captions are typically 1–2 sentences — a `too_thin` risk analogous to Schonbek PDPs; enable only if proven valuable, and reserve `web_projects` as a future option if volume grows); `/na/resources` → `web_resource` (catalog/PDF literature index), skip, same treatment as waclighting `/literature/*`; `/na/contact` → junk (the bare-`contact$` denylist entry above); `/na/products`, `/na/products/{indoor|outdoor}`, `/na/products/{indoor|outdoor}/{categoryId}` → `web_category`, skip. **The 4th path segment on category URLs is a LIVE FILTER FACET, not a vanity slug** (verified: `/na/products/indoor/12` = 13 cards page 1/7; `.../12/FQ` = a DIFFERENT 13-card set; a bogus segment renders an empty "No products match the selected filters." grid; real facet values are enumerated by the page's own filter links) — treat exactly like a filter query param: **skip/don't-store as facet junk, and NEVER fold into the base category URL as a "duplicate"** (it isn't one — collapsing would mislabel a genuinely different subset, or collapse an empty page into real content). Dropping faceted paths from the frontier is safe for coverage ONLY because the unfiltered listing's `?p=N` pagination surfaces every product in the category — **and that pagination IS traversed: the A-section per-host `?p=` exception explicitly covers base category listing paths (audit: `/na/products/indoor/12` = page 1 of 7), not just /projects and /news, so pages 2..N are actually fetched and their product-detail links/card evidence enter the frontier.** Faceted variants never inherit the `?p=` exception (A). State it as that trade-off. PDPs: `/{region}/product-detail/{numericId}` → `web_product`, **off by default**; brand-new URL shape needing its **own regex `^/(na|int)/product-detail/(\d+)/?$` in the shared taxonomy module** — incompatible with `SLUG_RE` (no slug exists) and with the `?s=` search resolver (no search endpoint; IDs are enumerable only from category listings, via the traversed `?p=N` pagination above). PDP evidence, when enabled, crawls BOTH regions' product-detail spaces (A). Soft-404 title-match rule per A. **Site bugs → web team, no plan change:** every page declares `<html lang="zh-CN">` despite all-English content (never rely on the lang attribute); the `/na/projects` listing hardcodes `/int/project-detail/{id}` links regardless of region (cosmetic — both regions' detail URLs resolve identically; BFS just follows the literal hrefs, but the /na detail namespace is orphaned from its own listing).
- **waclighting.com** — largest, 3 `product-sitemap*.xml`. `/product/<slug>/`→reconciliation (E). Category = **`/product-category/` prefix, arbitrarily deep** → `web_category`, skip. Capability = **nested `/applications/{architectural,commercial,hospitality,institutional,landscape,office,outdoor,retail,residential}/`** (NOT top-level) → `web_capabilities`; all in `page-sitemap.xml` (no extra mechanism). Content = **flat bespoke slugs, no `/about` prefix** (`/ourstory/`=About, `/warranty/`,`/faq/`,`/education/`, etc.) → default-allow-with-denylist over page-sitemap. Sub-line marketing (`/wac-home/`,`/dweled/`,`/ventrix/`,`/colorscaping/`)→`web_capabilities`. `/catalog/`→`web_category`. `/blog/*`→`web_news`. `/literature/*` (~20 thin catalog landers)→low-authority `web_resource`, skip. **Dev/ops denylist:** `test`,`admin`,`import`,`delete`,`get-`,`api`,`sitemap`,`salesrep-`,`thank-you`,`coming-soon`,`file-list`,`developer`,`-page-\d`,`configuratortool`,`csdemokit`,`pop-in`. **Sitemap-index skip:** `wtb_online_vendor-sitemap.xml`,`author-sitemap.xml`.
- **modernforms.com** — `/product/<slug>/`→reconciliation. **ALLOWLIST page-sitemap.xml** (polluted with `/reports/*`,`/admin-*`,`*importer*`,`/data-checker/`,`/ppidjump/`,`/qrcodes/`,`/contact-forms-submissions/`): allow `/who-we-are/`,`/fantechnology/`,`/warranty/`,`/fans/{integration,over-the-air-updates,smart-advice,support-videos}/`, the FAQ cluster `/smart-fans?-faqs-*` (**dual-spelling pattern — the site ships TWO real, differently-pluralized FAQ slugs: `/smart-fan-faqs-smart-advice/` [singular "fan"] and `/smart-fans-faqs-integration/` [plural "fans"]; a bare `/smart-fan-faqs-*` matches only the first and would silently drop the second as junk under the allowlist-only policy. The optional-`s` pattern `/smart-fans?-faqs-*` matches BOTH spellings — equivalently, list both literal slugs explicitly**), `/blog/*`,`/nature/*`,`/product-registration/`,`/where-to-buy/`. **News by `post-sitemap.xml` provenance** (12 flat slugs). `/nature/{spanish-alabaster,wood,marble}/`→`web_technology`. `/product-category/`→`web_category`.
- **schonbek.com** — **PDP `/product/<slug>/` CONFIRMED; lift rev.3 "report-only" hedge.** `product-sitemap.xml`+`product-sitemap2.xml`=**1,240 PDPs — parser must iterate BOTH.** Category pages carry static `href="/products/?product=<slug>/"` (BFS works) — **B.2: product sitemaps are the sole authoritative PDP frontier source; `?product=` is evidence-only** — resolve via its `<link rel=canonical>`, **never insert as a frontier row** (distinct legacy page → double-crawl). Content: `/our-story/`→`web_company` (heritage inside; no tech split); `/who-we-are/`=stale near-dup (dedupe); `/blog/`+dated+`/stories/`+`/press/`→`web_news`; care cluster (`/education/`,`/lighting-care/`,`/crystal-care/`,`/finish-care/`,`/warranty/`)→`web_faq`/`web_warranty`; `/literature/`=spec-sheet index, skip; `/brands/{beyond,signature,forever}/`. `?brand=`=facet junk. **Modern-Forms content on schonbek's sitemap: SKIP — CONFIRMED by Davis 2026-07-20** (no longer open; stale mirror, modernforms crawled directly). **page-sitemap.xml denylist (rev.3's single regex missed most evidence):** literal `{ppidjump,qrcodes,site-map,image-importer,curated-order-lookup}` + `/(^(admin|reports?|thank-you|rsvp|test|rss)[-_])|([-_]reports?($|[-_]))/i` + `/(-old($|[-_]))|(_$)/i` + literal `{press-2,new-home-page,who-we-are}`; **scoped to page-sitemap.xml only** so numbered PDP slugs (`adley-2`) aren't caught.
- **aispire.com** — **group-affiliated brand, one level below the 4 mains (Davis 2026-07-20); full crawl treatment, brand-corporate authority tiered at 1.1 (D).** **Classify PDPs by path regex `^/product/<slug>/$`, NOT sitemap membership** (`product-sitemap.xml`'s first entry is shop root `/shop/`→explicit SKIP_SLUGS). Content: **`/company/` (not `/about/`)→`web_company` @ tier 1.1**; `/blog/`→`web_news`; category skip. Denylist `/resources-old/` (stale dup). **Host denylist `support.aispire.com`** (Zendesk `/hc/en-us`).
  **Comparison-page policy (replaces the v1 blanket guardrail-based denylist rationale; verdict identical in both amendments — adopted, see merge note; Davis's condition: ingest only if NO specific competitor companies/products are referenced).** `/comparison/` (WP page 2037) and `/abicuscomparison/` (WP page 2040): **DENYLISTED IN FULL — verified against live content 2026-07-20 (policy final).** Analyst verification: both pages' ENTIRE substantive content (comparison table AND the FAQ copy) is a single 4000×8333 raster image (`/wp-content/uploads/2024/02/ABiCUS_Competitive_Product_Comparison_FEB_2024_FINAL.jpg`) — no extractable HTML text. The chart's "MARKET - GENERIC"/"MARKET - CONTROL" columns show legible competitor names/logos: **Legrand** (DMX Splitter), an ENTTEC-style **"D|NTEC"** DIN-rail gateway, **Engineering Solutions, Inc.** (RS-232 DMX Engine, response-box.com), **American Lighting** (ARTNET-DMX-GTWY), plus a third-party "DMX 4 PRO DIN" module. That fails Davis's condition ⇒ **both URLs stay denylisted in full.** **No partial-ingest rule exists:** there is no separable safe text/DOM (the HTML wrapper is nav/header only, zero product value), and the image asset itself must never be ingested. Correctness notes: **ABiCUS is aiSpire's OWN product** (their DIN-rail DMX gateway), not a competitor; the FAQ's "Modern Forms Smart Fans" mention is sibling-brand cross-sell, fine on its own but unreachable (rasterized into the same image). Sitemap check confirms these are the only comparison-type pages sitewide (no `/vs/`, `/compare/`) — no further URLs need review. **Optional out-of-band path (the only route to this content, out of crawl scope):** manually author a competitor-free curated summary of the ABiCUS FAQ content (Ethernet/PoE setup, Profiles, Presets, File Sharing, Web UI Zone/Group/Node config, auto DMX addressing) and ingest that as a separate curated document — never the live URLs.

### C. Extraction & chunking

`extractPage.ts` = `linkedom`+`@mozilla/readability` isolating main content, dropping nav/header/footer/aside/form/cookie/mega-menu → existing `htmlToText` (verbatim) → `chunkText`. **Do NOT gate on `<main>`/`[role=main]`** — wacgroup's `twentytwentyone` theme has none and splits content across multiple `<article>` blocks; **fall back to Readability over full `<body>`.**

**Metadata (audited hazards):** **take FIRST occurrence of each `og:` property** (wacgroup emits a second homepage-describing block; last-wins corrupts per-page title/canonical). **Ignore `og:description` where it is a static sitewide tagline** (all 7 wacgroup pages identical) — derive summary from body/`<title>`/`og:title`, never dedup on it. **Breadcrumb: Yoast JSON-LD `BreadcrumbList` PRIMARY** (wacgroup/waclighting/schonbek), visible `.breadcrumb` fallback; **guard pure-numeric segments** (modernforms `8500`) and **dual parents** (metadata-only). **Freshness:** JSON-LD `dateModified`/`article:modified_time`→`published_at` (wacgroup: `article:published_time` absent). Normalize `WACGROUP`→"WAC Group"; domain wins over `og:site_name`. **wacarchitectural exception (whole domain — the WordPress/Yoast rules above have nothing to key off):** ZERO `og:*` tags, ZERO JSON-LD (no BreadcrumbList, no dateModified), no canonical tags, on ANY page checked. There, breadcrumb comes ONLY from the visible in-page `>` trail in the DOM (e.g. `Products > Indoor > Recessed > {title}`; project pages literally embed `Projects > Museums & Galleries Lighting > Almaty Museum of Arts`), with Readability/title-only summarization as the floor; **`published_at` stays null permanently for every page on this domain — no signal exists.** Also do not trust its `lang` attribute — every page declares `lang="zh-CN"` despite all-English content (cosmetic site bug). **Confirmed site bugs to report to the web team (no plan changes):** (1) wacgroup `/commercial/` breadcrumb leaf reads `Architectural` — Davis confirmed 2026-07-20: real cosmetic bug; (2) wacarchitectural `lang="zh-CN"` on all pages; (3) wacarchitectural `/na/projects` hardcoded `/int/project-detail/*` links (per B).

**On-page code harvest (feeds E).** PDPs: asset URLs→`codeFromAssetUrl`+slug tokens→`model_codes[]`; `extractSpecSheet`→`discovered_spec_sheet_url`. **Schonbek exception:** static PDP HTML has zero asset src/href → harvest from `<title>`/JSON-LD (E.2). **aiSpire:** prefer/tag the page's own **primary designated code** (spec-table Order Number/title, e.g. `A2RU-447-27`) over incidentally-linked accessory codes (modular housing/trim/driver PDPs). **wacarchitectural: model codes are a NEW shape** — short letter-prefix + digits, no hyphens, built from a spec/configuration table (e.g. `ADDT25`/`ADDT35` = Model ADDT + Size 2/3 + Trim 5; also `ASL11S`–`ASL13S`, `AME19F`, `ATL41HLD`–`ATL43HLD` on category cards) — closer to the fan-family config-table axis than waclighting's hyphenated `FR-W1801` style; `deriveModelCodes()`/`model_codes` needs a new pattern family for this brand. Asset filenames on this host have **no reliable convention** (see E.3), so asset-kind classification keys on anchor text/DOM position. Pages not chunked (F).

**Row shape.** One `kb_documents`/URL: `source_system='web_crawl'`,`external_id=canonical url`,`url`,`title`,`brand`,`scope='public'`,`doc_type`+`authority`,`r2_key`,`content_hash=sha256(normalized main text)`,status omitted. Header `"{brand} — {title}\n{breadcrumb}\n{summary}\n{Published}\n\n{main}"` before `chunkText`; `page=null` (Published line omitted where `published_at` is null, i.e. all of wacarchitectural).

### D. Schema, source_system & authority

`source_system='web_crawl'`. **doc_type (open text, API-validated):** `web_company`,`web_capabilities`,`web_technology`,`web_news`,`web_faq`,`web_warranty`,`web_category`(opt),`web_resource`(literature/catalog landers),`web_materials`(opt or fold into web_technology),`web_product`(off default). (wacarchitectural project-detail pages ride `web_capabilities`, opt-in off — see B; `web_projects` reserved as a future option, not added now.) **Code change:** `tools.ts::searchDocs` adds all web types to BOTH public+internal allowlists via shared `WEB_DOC_TYPES` const; update tool description to mention "company/capabilities/technology/news pages."

**Migration `0054` (incl RLS):**
1. `kb_documents.authority real not null default 1.0` (neutral → ordering provably identical at rollout); NOT denormalized to `kb_chunks` (applied post-fusion in `kb_search` outer projection via join to parent).
2. `crawl_frontier` table, RLS mirroring `pdp_urls` (`is_active_internal_or_admin()` select, no write policy).
3. **`kb_search` rewrite — bounded, band-gated, ADDITIVE authority bias (see D.3 for the design decision).** Same RRF branches + per-branch LIMITs unchanged. Two NEW parameters with defaults so the whole weighting is tunable **without a further migration**: `authority_weight real default 0` (λ) and `authority_band real default 0.85` (β). SECURITY INVOKER, re-grant.
4. **Brand-filter predicate: LEFT UNCHANGED** — `(brand_filter is null or c.brand = brand_filter)`. The rev.3 force-include `or c.brand='WAC Group'` is **REMOVED from the plan** (it caused objection-3 leakage). Corporate content is surfaced by the tool/intent layer choosing `brand_filter = NULL` on company/capabilities queries, not by a SQL predicate that injects corporate chunks into every brand-narrowed query. `product_semantic_search` untouched.

**D.1 — kb_search ordering (concrete SQL shape).** After the RRF `fused` CTE (unchanged: `f.score = sum(1.0/(60+rank))` over the two per-branch-LIMIT-50 branches), add:
```
with fused as (…unchanged…),
pool as (
  select f.*, d.authority,
         max(f.score) over () as max_score
  from fused f
  join kb_documents d on d.id = f.document_id
)
select …,
  (f_score
     + case
         when authority_weight > 0
          and score >= authority_band * max_score      -- band gate
         then authority_weight * greatest(authority - 1.0, -0.3)  -- additive, signed, clamped
         else 0
       end
  )::real as ranked_score
from pool
order by ranked_score desc
limit match_count;
```
- **Band gate (β, primary swamping guard).** Bias applies ONLY to rows whose fused score is within `β=0.85` of the pool maximum. A weakly-relevant corporate chunk sitting mid-pool (the exact objection-1/2 failure mode) is **outside the band and receives exactly zero bias** — it cannot float on authority alone.
- **λ (magnitude, second guard).** Default `authority_weight = 0` at migration time (⇒ bias=0, ordering byte-identical to pre-0054). Enabled at λ=0.004 by the tool layer only for company-shaped queries (D.2). Top authority delta is +0.5 (1.5−1.0), so **max positive bias = 0.004 × 0.5 = 0.002** — smaller than the ~0.003 fused-score gap between a rank-1 and a rank-5 in-pool result, so the bias can only reorder near-ties, never leapfrog a clearly-more-relevant chunk. Negative authorities (web_product 0.8, resource/nav 0.7) get a small negative nudge, still band-gated.
- Both λ and β are **function parameters, not literals baked into the migration** — recalibration after launch is a config change in `tools.ts`, not a new migration. This directly answers "the form/weights are locked in SQL."

**D.2 — Intent gating (tool layer, `tools.ts::searchDocs`).** kb_search stays generic; the caller supplies λ and brand_filter based on cheap query-shape detection:
- **Product/SKU-shaped query** (SKU/model regex hit, e.g. `FR-W1801`, or a brand_filter set together with product-oriented doc_types): pass `authority_weight = 0` (authority OFF) AND keep the narrow `brand_filter`. Result: no authority bias at all, and — with D.4's predicate — **zero WAC-Group corporate chunks injected**. Fully closes objections 1 and 3 for the queries that matter.
- **Company/capabilities-shaped query** ("who is WAC Group", "capabilities", "sustainability", "technology"): pass `authority_weight = 0.004` and `brand_filter = NULL` so corporate content is eligible and the small in-band bias lets a higher-authority corporate page win a near-tie (Goal 5).
- **Ambiguous/default:** λ=0.004 but band gate + 0.002 cap keep it a tiebreak.
- Edge case (internal user filtered to a brand asks a corporate question): searchDocs may issue a second `brand_filter=NULL` corporate-scoped call and merge, rather than forcing corporate into the brand-filtered call. This keeps narrow brand/product retrieval clean.

**D.3 — Why additive+band, not multiplicative (design justification, per objection 1a/2).** Verified against 0043: fused RRF = `sum(1/(60+rank))`, per-branch LIMIT 50 ⇒ pool raw scores span ≈0.0091 (rank-50, one branch) to ≈0.0328 (rank-1, both) — a ~3.6× spread, with strong-vs-mediocre in-pool gaps of only 1e-3–1e-4. A **multiplicative** factor in [0.7,1.5] injects a ~2.1× spread that is comparable to the *entire* relevance signal, so it rewards being-in-pool over relevance-margin: a 2nd/3rd-best corporate chunk (≈0.0325×1.5≈0.049) beats the genuinely-best product/technical chunk (≈0.0328×1.0), i.e. a silent relevance regression on the very product/photometric queries Goals 3/4 depend on. It also grows the boost with the base score and can't be tuned below the inter-rank gaps. **Additive** gives a fixed, base-independent nudge that we deliberately size (λ) to a *fraction* of the smallest meaningful RRF gap, so authority acts as a tiebreak. **Band-gating** confines even that nudge to already-highly-relevant results, structurally preventing an off-topic high-authority chunk from being lifted. **Intent-gating** removes authority entirely on product/SKU queries. Multiplicative was rejected on these grounds; additive+band+intent is the chosen form.

**Authority tiers** (per-(domain,path), one `AUTHORITY_TIERS` map; values are the raw `authority` stored on `kb_documents`, and enter retrieval only as the clamped delta `authority−1.0` under D.1). **The tier ladder encodes the brand model directly. Merge note: the map uses EXPLICIT per-(domain,path) enumeration (Amended A's form), not generic path patterns (Amended B listed `/about`,`/company`,`/ourstory`… generically) — a generic `/company` pattern would match `aispire.com/company/` and promote it to 1.2, contradicting the 1.1 tier both amendments intend; explicit enumeration cannot self-contradict.** Tiers: WAC Group corporate (all 7 wacgroup pages) **1.5**; **MAIN-brand corporate/company/capability — the 4 main brands ONLY** (waclighting `/ourstory` + `/applications/*` + brand `/technology`, modernforms `/who-we-are`, schonbek `/our-story`, **wacarchitectural `/na` + `/na/about`**) **1.2**; **aiSpire brand-corporate (`aispire.com/company/` and any future aiSpire company/capability paths) — NEW tier 1.1**, inserted between main-brand-corporate (1.2) and the marketing-landing baseline (1.0), concretely encoding Davis's "aiSpire sits slightly below the 4 main brands" within the additive/band-gated model without disturbing any other tier's ordering (Δ=+0.1 ⇒ max bias +0.0004 at λ=0.004 — an even smaller tiebreak than the mains' +0.0008; **the `AUTHORITY_TIERS` map must carry an explicit comment naming this as the group-affiliated tier per Davis 2026-07-20**); brand marketing landing (=spec-sheet baseline) **1.0**; news/blog **0.9**; `web_product` **0.8**; `web_resource`/category/nav **0.7**. Goal-5: on a company-intent query, wacgroup/about (Δ=+0.5 ⇒ +0.002) outranks any main-brand about (Δ=+0.2 ⇒ +0.0008), which in turn outranks aispire/company (Δ=+0.1 ⇒ +0.0004), when all are in-band and near-tied — the intended group > main-brand > affiliated ordering, achieved as a tiebreak, not a swamp. **All 4 MAIN brand domains are first-party at the 1.2 brand tier — schonbek NOT downgraded, wacarchitectural NOT provisional; aiSpire alone sits at 1.1.** **Cross-brand bleed rule (confirmed):** Modern-Forms content on schonbek's sitemap is SKIPPED (Davis 2026-07-20), never tiered under Schonbek.

**D.4 — Worked contamination example (documents the guard).** Query "FR-W1801 warranty" (product/SKU-shaped ⇒ λ=0, brand_filter set). Best warranty chunk: rank-1 both branches, fused ≈0.0328, authority 1.0. Off-topic wacgroup `/about` chunk that happens to land mid-pool: rank-12 one branch, fused ≈0.0143, authority 1.5. Old multiplicative worst case (2nd-best corporate near top): 0.0325×1.5=0.0488 > 0.0328 ⇒ corporate WINS (the bug). New model: λ=0 on this query ⇒ **bias=0 for every row ⇒ warranty chunk ranks first**; and even under the ambiguous path (λ=0.004) the mid-pool corporate chunk is outside the 0.85·max band ⇒ bias 0 ⇒ still loses. This example is encoded as regression tests I.T1/I.T4 below.

### E. Product-page-linking cleanup

`reconcilePdp.ts` behind `--reconcile-pdp` (dry-run first), consuming `crawl_frontier` evidence (no second fetch). wacgroup contributes zero rows (documented).

**E.0 Shared helpers (prereq).** Move `DOMAIN`,`BRAND_NORMALIZATION`,`SLUG_RE`,`SKIP_SLUGS`,`SCHONBEK_SUB_BRAND_SLUGS`,`extractSpecSheet`,`schonbekSpecSheet`,`canonicalBrand`,`PdpProduct` from `apps/products-sync/src/pdp.ts` → new `packages/shared/src/pdp/taxonomy.ts`; products-sync re-imports (tests guard); crawl imports same. **Modern Forms `extractSpecSheet` data-ppid branch confirmed WORKING live — no products-sync bug, no fix owed.** **NEW: add `"WAC Architectural": "wacarchitectural.com"` to the shared `DOMAIN` map** (currently absent from both crawl needs and `products-sync/pdp.ts` — see E.2b), with **canonical host = www** (opposite of waclighting/modernforms), plus the wacarchitectural PDP regex `^/(na|int)/product-detail/(\d+)/?$` as its own pattern alongside `SLUG_RE` — the existing `SLUG_RE` (`/product/([a-z0-9-]+)/`) does NOT match this shape; a numeric ID is not a slug; do NOT overload `SLUG_RE`.

**E.1 Reverse code→SKU index.** Page `products` (`sku,brand,family,variants,primary_image_url,image_urls,ies_url`); candidate codes = `deriveModelCodes(row)` ∪ `variants[].sku` ∪ own `sku`, normalized to resolver's `dedupe` key → `Map<codeKey,Set<sku>>`. Same function/columns = one vocabulary. **Fixtures must include a fan-family shape** (`FR-W1801-60L`→per-Kelvin/CRI `FR-W1801-2700K-90`), a distinct axis from lighting finish codes, **and a wacarchitectural config-table shape** (no-hyphen letter-prefix codes: `ADDT25`/`ADDT35`) once the new `deriveModelCodes` pattern family lands.

**E.2 Match PDP→SKU.** Look up `model_codes[]`+slug tokens, union sets → `one_sku` (auto-eligible) / `unresolved` (report) / `collision` (never auto-write per-SKU: all one family→family candidate; multi-family→review-log). Collisions expected (finish-agnostic `gatedTruncate`; **aiSpire modular PDPs → higher cross-family rate**; aiSpire variable-products collapse to one code, no collision). **Schonbek:** code from `<title>`/JSON-LD `{Family}|{PPID}|{SubBrand}|…`; **title→PPID ~60% on sampled sitemap, NOT 1:1** (~40% bare `{Family}|Schonbek`; JSON-LD `name` mirrors title, no independent fallback). Extractor: (1) regex PPID + exact `products.sku`; (2) fallback family-name match for bare titles; (3) unresolved recorded, not dropped. **Sample ≥15–20 from `product-sitemap2.xml`'s 240 unsampled URLs before promoting ~60% to a sitewide figure.** Schonbek = highest-value target (excluded from `SCRAPEABLE` in `pdp.ts:55` → 100% cold-start gap). **wacarchitectural: evidence-collection only** (frontier `model_codes[]` from category cards/PDP spec tables, both regions — the category-card arm depends on the A-section `?p=` exception covering base category listings, so evidence comes from ALL pages of each category, not page 1 only); no matching/writes until its dedicated resolver exists (E.2b).

**E.2b — wacarchitectural E-scope (audited — Davis's open question CONFIRMED).** wacarchitectural.com is completely ABSENT from products-sync's `DOMAIN`, `BRAND_NORMALIZATION`, AND `SCRAPEABLE` in `apps/products-sync/src/pdp.ts` (grep zero matches repo-wide) — a **100% cold-start gap worse than Schonbek's** (Schonbek at least gets search-only handling; WAC Architectural gets nothing); `pdp_urls` does not currently cover this domain at all. More importantly, the WIES-derived resolution method (slug regex + `?s=` search-then-scrape) is **STRUCTURALLY INCOMPATIBLE** with the site: PDPs are `/product-detail/{numericId}` Blazor route params with no slug, there is no `?s=` search endpoint, and IDs are enumerable only by crawling category listing pages — **whose `?p=N` pagination the A-section traversal exception now explicitly covers (extended in v2.1 to base category listing paths precisely so this enumeration sees pages 2..N of every category, not just page 1; without that extension the global bare-`p` strip would have silently truncated the harvest — and since E.2's evidence collection is ACTIVE in v1 (report-only), the extension is load-bearing immediately, not only when PDP ingestion is eventually enabled).** Therefore: **v1 reconciliation scope for this brand = evidence harvest into `crawl_frontier` only (both regions), report-only; a dedicated resolver (product-name/model-code match against listing/PDP text over both regional catalogs, not slug regex) is logged as a distinct follow-on work item — do NOT fold it into E.0's shared module as-is.** E's write paths in this plan do not touch this brand. Soft-404 stub pages ("Not Found | WAC Architectural", HTTP 200) must be excluded from evidence by title match. **Adjacent pre-existing bug, flag to team (not this plan's fix surface):** `packages/shared/src/productinfo.ts` recognizes 'WAC Architectural' as a CANONICAL_BRAND (via BRAND_ALIASES) but it is ABSENT from `BRAND_SITES`, so `brandSite()`/`canonicalUrlFor()` silently falls through to the waclighting.com default — canonical URLs are generated against the wrong domain for WAC Architectural products wherever that helper is used.

**E.3 Reconcile `pdp_urls` (heal, never clobber).** Only `one_sku`: null→set canonical; `''`→fill; differs→**propose to review log** (WIES authoritative) except auto-fix a legacy `?s=` fallback. `spec_sheet_url` null→populate. **Static-href-first spec harvest:** prefer `_SPSHT.pdf` (waclighting `/storage/.../specsheet_pdf/<CODE>_SPSHT.pdf`; modernforms `/images/products/SPECSHEET_PDF/<MODEL>_SPSHT.pdf`; aiSpire S3 `<base>_SPSHT.pdf`); **fallback** `?download=specs[N]` reading `Content-Disposition` (waclighting) / `data-ppid`→`dynamic-specsheet` (modernforms, reuse working branch) / `schonbekSpecSheet(subBrand,ppid)` PHP template (schonbek only source; blocked for ~40% bare-title PDPs until E.2 fallback). **wacarchitectural exception: the `_SPSHT.pdf` convention does NOT generalize (audited — direct sampling of PDPs 968/970/955/950/940/930):** one `_SPSHT.pdf` match (968: `/uploads/files/202607/968_ADDT25_35_SPSHT.pdf`) is the exception, not the rule — observed variants include `_SPECS` (`FQ_2IN_MILLWORK_HOUSING_SPECS.pdf`, no product-ID prefix), `_INSSHT.pdf` **installation sheets (a distinct asset kind)**, human-readable `..._C.pdf` cutsheets (`ExterminatorII_1014_W_Track_C.pdf`, `RING_RAL6_STATIC_C.pdf`), and opaque hash-named `.pdf`/`.zip` bundles (likely IES/dimming) under `/uploads/files/{yyyymm}/`. **No reliable filename convention exists** — asset-kind classification on this host must key on anchor text/DOM position, with `_INSSHT`/`_SPECS`/`_C` recorded as observed suffix variants (feeds the third-asset-kind item in risks). **`ppid` excluded from model_codes[].** **Retired/EOL guard:** do NOT auto-heal from a PDP marked Retired (waclighting `solorail-14`) → review log.

**E.4 `product_documents` (SKU + family).** *0046 gotcha:* SKU index is non-partial (upsert `onConflict:document_id,product_sku` valid); **family index is PARTIAL (`WHERE product_sku IS NULL`) → cannot be an ON CONFLICT arbiter.** SKU-level: healed `spec_sheet_url` flows through existing `syncPdpSpecSheets`. **Family-level — NEW `syncFamilySpecSheets`, gap-only, NO upsert:** ensure doc via real `(source_system,external_id)` upsert; SELECT existing `(document_id,family)` → `coveredFamily`; `.insert()` only gap rows (`product_sku:null,family:<resolved_family>,scope:'public'`), **no `onConflict`**. `resolved_family` must be a real `products.family` value; span-families or none→per-SKU only. `dryRun` short-circuits.

**E.5 WIES.** products-sync stays authoritative per-SKU resolver; crawl only fills nulls/empties + proposes overrides. (wacarchitectural: nothing to be authoritative over yet — see E.2b.)

**E.6 Financial guardrail.** URLs/slugs/doc-links/families only; no price/quote fields (none exist). Unit test asserts no `$`/price keys in any write payload.

### F. Scope & brand tagging

**Scope=`public`** every crawled row (asserted; no path emits internal). **Brand by domain** (inverted shared `DOMAIN`): wacgroup→WAC Group; **wacarchitectural.com→WAC Architectural (new entry, MAIN brand)**; waclighting→WAC Lighting; modernforms→Modern Forms; schonbek→Schonbek; aispire→aiSpire (group-affiliated tier, see D). Corporate rows carry `brand='WAC Group'` and are surfaced by the tool choosing `brand_filter=NULL` on company-intent queries (D.2) — **not** by a SQL force-include; a narrow brand/product query does not pull them. Domain wins over `og:site_name` (moot on wacarchitectural — no og tags at all). **Per-path override (schonbek): CONFIRMED — `/fans/*` + MF-catalog slugs are SKIPPED (Davis 2026-07-20; stale mirror, modernforms crawled directly)**; Beyond/Signature/Forever still normalize to Schonbek. **wacarchitectural region dedup:** `/int` content-page mirrors of `/na` are near-dups on EXTRACTED PROSE (not raw bytes) → skip; `/na` vs `/int` product-detail pages are NOT dups (distinct regional catalogs) — never cross-region-dedup PDPs (moot for chunking; PDP prose off by default). **Dedup:** PDP HTML not chunked (facts in `products`+spec sheets; `web_product` opt-in off in rollout); PDFs never web-ingested (feed E; `covered`/`coveredFamily` prevent dup product_documents); help-center hosts denied (`support.aispire.com`) — **verify aiSpire Zendesk org is already synced under `source_system='zendesk'`** (`zendesk_article` is the doc_type); if separate/untracked → flag as out of scope. `unique(source_system,external_id)` = one row/URL.

### G. Embeddings & retrieval

Reuse `embed()` bge-m3 1024-dim + `toVectorLiteral` — same column/HNSW index, no new vector migration. `kb_search` serves web chunks once `active` AND doc_type in `searchDocs` allowlist. Denormalized scope/doc_type/brand from parent at insert. **Citations:** `url`=canonical live page → builder unchanged ("{title} — {url}"); `page` null. Optional: doc_type labels, `published_at` for news (never populated for wacarchitectural — no freshness signal exists on that domain).

### H. Idempotency & failure

`content_hash` (normalized main text) = idempotency key; capture omits `status` (changed→pending_extract, chunks deleted+re-inserted; unchanged→active). Checkpoints: `crawl_frontier.status` + `kb_documents.status`. Reconciliation idempotent (indexes rebuilt, outcome persisted; `covered`/`coveredFamily` gap-only; E.3 fills only null/empty). R2 store-at-capture (`R2_*` secrets), Step B re-fetch fallback. **`last_error` taxonomy:** `js_shell`,`too_thin`(failed, don't embed noise; schonbek PDPs + wacarchitectural project-detail captions expected-thin), transient 5xx (backoff), permanent 4xx→failed, `robots_disallow`→skipped, 404/410→superseded, **NEW `bot_block`** for `cf-ray` 403 on a page that resolves fine to honest UA (retry/alert, NOT permanent failed) — waclighting/modernforms/aispire are Cloudflare Bot-Managed; **wacarchitectural is plain nginx (no Cloudflare) — `bot_block` not expected there**; **plus the wacarchitectural soft-404 title-match rule (A) so "Not Found" stubs land superseded, not ingested.** `chunkText.maxChunks` bounds poison pages. `--retry-failed` requeues.

### I. Rollout

**Order** *(merge note: adopts Amended B's 9-step sequence — aiSpire enable gets its own step rather than riding the "brand sites" step, because its enable carries two verifications no other site has: the comparison-page full-denylist and the 1.1 tier; folding it into step 5 (Amended A) would leave those unverified at the only moment they can be cheaply checked)*:
1. Land E.0 move (pure refactor, products-sync tests guard) — **including the wacarchitectural `DOMAIN` entry + the new numeric-PDP regex** (classification only; the brand's dedicated resolver is a logged follow-on, not in this rollout).
2. Land `0054` (default `authority_weight=0` ⇒ ordering byte-identical to pre-0054, regression-test known queries) + `searchDocs` allowlist/`WEB_DOC_TYPES`/tool-desc; 0054 does NOT touch `product_documents` (family path gap-only insert). Brand predicate unchanged (no force-include).
3. Merge crawl modules **dark** (incl. `tools.ts` intent-gating from D.2, but gated so λ stays 0 until enabled) — includes the seeded-BFS frontier mode, the robots/sitemap HTML-shell "absent" detection, the per-host `?p=` traversal exception (all three wacarchitectural listing shapes: /projects, /news, base category listings), and per-host canonical-host config (wacarchitectural keeps www), all unit-tested before any live enable.
4. **Live sample = wacgroup.com only** (`--limit 50 --dry-run` then real) — tiny (~7 pages, ~15–30 chunks), 100% coverage, ideal pilot; verify no nav/footer bleed, `authority=1.5` stored, first-occurrence og, JSON-LD breadcrumb, citations. **Positive check:** "WAC Group capabilities" (company-intent ⇒ λ=0.004, brand_filter NULL) returns corporate chunks. **Contamination check (blocking):** with only wacgroup corporate content plus a seeded/known product+warranty chunk present, "FR-W1801 warranty" (product-shaped ⇒ λ=0) returns the warranty chunk first and NO wacgroup `/about` chunk above it.
5. Enable brand content pages on the WordPress mains (waclighting, modernforms, schonbek; content taxonomy only; hold web_product/category) — **watch `cf-ray` 403 bot-blocks**; goal-5 tiebreak: on a company-intent query waclighting/about (Δ+0.2) < wacgroup/about (Δ+0.5); on a Schonbek-brand product query, confirm zero WAC-Group corporate chunks surface. **modernforms content-page enable must confirm BOTH FAQ slugs (`/smart-fan-faqs-smart-advice/` and `/smart-fans-faqs-integration/`) landed** — a dropped plural slug is the objection-1 regression.
6. **Enable wacarchitectural.com (last main-brand content site, first BFS-only host).** Sequenced after the sitemap-driven sites prove out because it exercises the new machinery end-to-end. Verify: apex→www normalization; robots/sitemap correctly detected as "absent" (no HTML parsed as robots/sitemap); `/` never fetched (no geolocation nondeterminism); `?p=` pagination followed on /projects and /news so ALL 4/7 listing pages traverse and every news-detail/project-detail URL lands in the frontier; **`?p=` pagination ALSO followed on base category listings — sample at least one multi-page category (e.g. `/na/products/indoor/12`, 7 pages) and confirm ALL its pages traverse and every product-detail ID appearing on pages 2..N lands in the frontier (evidence rows), not page 1 only; confirm faceted 4th-segment variants remain excluded from the exception (no `.../12/FQ?p=` rows in the frontier)**; /int content pages deduped against /na at prose level (about/news/resources/projects skipped as /na dups); category facet 4th-segment paths skipped as facet-junk and never folded into the base category URL; product-detail soft-404s ("Not Found | WAC Architectural" on 200) excluded/superseded; visible-`>`-trail breadcrumbs extracted (no JSON-LD exists); `published_at` null on every row; `/na/about` stored at authority **1.2** (main-brand tier), `brand='WAC Architectural'`; project-detail held opt-in-off (too_thin). Company-intent tiebreak spot-check: wacgroup/about (1.5) > wacarchitectural/na/about at the same 1.2 band as the other three main-brand abouts > aispire/company (1.1).
7. **Enable aispire.com content pages** — `/company/` stored at authority **1.1** (group-affiliated tier); **confirm `/comparison/` + `/abicuscomparison/` never enter the frontier** (full denylist per B's concrete policy); goal-5 three-way check: aispire/company below both wacgroup (1.5) and every main-brand about (1.2) on company-intent near-ties.
8. **Step E report-only** — classify one_sku/family/collision/unresolved, diff vs `pdp_urls`/`product_documents` WITHOUT writing; review logs; then enable null/empty fills + family gap-inserts. **Schonbek report-only until `product-sitemap2.xml` sample confirms coverage. wacarchitectural excluded from E writes entirely — report-only/evidence-harvest indefinitely in v1 (E.2b; dedicated resolver is a separate follow-on).**
9. Flip `searchDocs` allowlist, widen sites, weekly cron. **Post-launch λ/β tuning is a `tools.ts` config change, no migration** (D.1).

**Testing.**
- `frontier.test.ts` — normalization, junk/legal-page classification, canonical dedup, **per-site fixtures for all 6**: wacgroup 7-keep/4-junk incl `/`; **wacarchitectural: apex→www keep-www (opposite-direction fixture vs waclighting's www-strip — proves normalization is per-host, not blanket), seed list excludes `/` (never-seed-root rule), `?p=` PRESERVED on /na/projects+/na/news traversal AND on base category listings (assert `/na/products/indoor/12?p=3` is kept as a distinct traversal URL, and pages `?p=2..7` of a category fixture yield distinct frontier traversal rows) but stripped elsewhere, **and NOT preserved on faceted variants (assert `/na/products/indoor/12/FQ?p=2` is skipped as facet-junk — the `?p=` exception does not apply to 4th-segment paths)**, category 4th-segment facet → skip-never-collapse (assert `/na/products/indoor/12/FQ` is NOT canonicalized to `/na/products/indoor/12`), bare `/contact` junk, `^/(na|int)/product-detail/(\d+)/?$` classification + `SLUG_RE` non-match, /int content-path dedup vs /na (content-mirror skip vs product-detail keep-both), product-detail soft-404 title exclusion**; waclighting `/product-category/`+`/applications/`+dev-ops denylist; modernforms allowlist + `post-sitemap` news-by-provenance + **explicit assertion that the `/smart-fans?-faqs-*` allowlist entry matches BOTH `/smart-fan-faqs-smart-advice/` (singular) AND `/smart-fans-faqs-integration/` (plural), i.e. neither confirmed FAQ page is dropped as junk — the regression test for objection 1**; schonbek `?product=` evidence-only + corrected page-sitemap denylist item-by-item + **MF-content skip (confirmed decision)**; aiSpire `/product/` regex + `/shop/` denylist + **`/comparison/`+`/abicuscomparison/` full-denylist fixture**; shared `SLUG_RE` identity, www→apex.
- `robots/sitemap.test.ts` — disallow parse, index recursion (schonbek BOTH product files), per-host Crawl-delay, aiSpire `/sitemap.xml` fallback, **wacarchitectural HTML-shell fixtures: an HTTP-200 `text/html` SPA-shell body at `/robots.txt` and `/sitemap.xml` is classified ABSENT (allow-all, no sitemap), is NEVER fed to the robots/sitemap parsers, and the host falls through to seeded-BFS mode.**
- `extractPage.test.ts` — no-`<main>` Readability fallback, **first-occurrence og**, **og:description-ignore**, JSON-LD breadcrumb + numeric/dual guard, PDP harvest (Schonbek title-parse, aiSpire primary-code preference), **wacarchitectural fixtures: zero-og/zero-JSON-LD page → visible `>`-trail breadcrumb extracted + `published_at` null + `lang="zh-CN"` ignored; soft-404 stub ("Not Found | WAC Architectural" @ HTTP 200) detected and excluded; prose-level (not byte-level) /na-vs-/int dup detection; new no-hyphen model-code pattern (`ADDT25`/`ADDT35`, `ASL11S`, `AME19F`) in `deriveModelCodes`; asset-kind classification by anchor text/DOM position with `_SPSHT`/`_SPECS`/`_INSSHT`/`_C`/hashed-name variants.**
- `authority.test.ts` — every (domain,path)→tier value **incl. wacarchitectural `/na`+`/na/about`→1.2 and aispire `/company/`→1.1, asserting the brand-model invariant: exactly the 4 main brands at 1.2, aiSpire strictly between 1.0 and 1.2, and the 1.1 < 1.2 < 1.5 ordering**; and the **pure bias function** `authorityBias(fused, maxScore, authority, λ, β)`: returns 0 when λ=0, 0 when below band, correct signed clamp within band, and max positive output ≤ 0.002 at λ=0.004/authority=1.5 (asserts the cap that prevents swamping).
- `reconcilePdp.test.ts` — reverse join, one_sku fill, collision→family / cross-family→review, unresolved no-write, propose-not-clobber, retired-PDP guard, fan-family fixture, **wacarchitectural no-hyphen-code fixture + assertion its rows are evidence-only (no pdp_urls/product_documents writes for the brand)**, Schonbek ~60%/fallback, **`syncFamilySpecSheets` uses plain `.insert()` NEVER `.upsert({onConflict})`** (guards 0046 partial-index bug) + gap-only idempotency + no cross-family force, **no `$`/price keys**.
- **DB-level retrieval tests (extend `anonBoundary`/`surface`) — the weighting failure mode, not just the tiebreak:**
  - **T0 (neutral):** `authority_weight=0` (rollout default) ⇒ ordering identical to a pre-0054 snapshot on a fixed query set.
  - **T1 (contamination / swamping — CORE):** seed a strongly-relevant LOW-authority product/technical chunk (rank-1 in both branches) and a weakly-relevant HIGH-authority (1.5) corporate chunk (mid-pool, one branch) for a product-shaped query; assert the product chunk ranks ABOVE the corporate chunk under λ=0.004 (band gate excludes the corporate chunk) AND under the product-intent path λ=0. This is the failure mode objections 1/2 name; it must be red before the model and green after.
  - **T2 (band boundary):** a corporate chunk just below `β·max_score` receives zero bias ⇒ ordering vs its authority=1.0 counterpart is unchanged.
  - **T3 (Goal-5 positive, in-band near-tie):** two near-equal-RRF corporate chunks, higher authority wins — but only when both are in-band (proves authority acts as an intended tiebreak, not a re-rank); **extend with the three-way group(1.5) > main-brand(1.2) > aiSpire(1.1) ordering on near-equal RRF.**
  - **T4 (brand-predicate interaction — objection 3):** a brand-filtered product query (`brand_filter='Schonbek'`) returns NO WAC-Group corporate chunks above the on-brand top hit; with the removed force-include, ideally returns no WAC-Group corporate chunks at all.
  - **T5 (equal-RRF tiebreak):** the original test kept, now clearly labeled as the trivial case (no longer the only coverage).
  - **T6:** public surface can't see internal; hash-stability; live family insert against the partial index succeeds, second run inserts zero.

**Observability.** `status`/`last_error` + ingest dashboard: discovered/captured/changed/extracted/failed(by reason incl `bot_block`)/superseded, reconciliation by `resolution_state`, writes vs proposals, and an expected **"0 PDP from wacgroup"** line **plus a "wacarchitectural: report-only/evidence-only, 0 pdp_urls writes" line, its soft-404-excluded/superseded count for the product-detail space, and a per-category pagination-coverage stat (pages traversed vs pages advertised by the listing's own `?p=N` links — a category stuck at 1/N is the silent-truncation regression the v2.1 fix closes).** SQL panel: `web_crawl` × doc_type × status + thin/failed list. **Authority panel:** for a fixed product-query probe set, log how often a `web_company`/`web_capabilities` chunk appears in the top-3 (target ≈0); regression-alert if it rises after a λ/β change.

**Cost/volume.** ~1.5–4k content pages → ~4–10k chunks (driven by brand sites; wacgroup ~7 pages but top authority; **wacarchitectural adds a modest content tail — about + ~7 news listing pages' worth of detail articles + resources index, with the 6 project captions and PDPs opt-in off — well under a hundred stored docs at launch; the category `?p=N` traversal adds only listing-page fetches (skip/evidence, never stored docs), a few dozen extra requests per region**). bge-m3 near-free; R2 few-hundred MB; crawl ~1–2h at ~1 req/s. Weekly re-crawl re-embeds only changed pages. **Do not bulk-ingest PDP prose** (waclighting/schonbek thousands of PDPs; wacarchitectural both regions); `web_product` opt-in.

**Risks / open questions.**
- **Authority weighting is a LAUNCH GATE, not post-launch tuning.** The model (D.1–D.3: additive, λ≤0.004, band β=0.85, product-intent disables λ, no SQL force-include) plus the DB regression tests T1/T4 must be green before web content is retrieval-enabled. Open tuning is limited to *values* of λ/β (config-only, no migration); the *form* and the failure-mode tests are non-negotiable and cover cross-doc-type (product-vs-corporate) and brand-predicate degradation, not just company-vs-brand ordering.
- Schonbek title→PPID ~60% on sampled file — family-name fallback + `product-sitemap2.xml` sample required before write-enable; report-only until then.
- **RESOLVED — Brand-roster mismatch (Davis 2026-07-20):** wacarchitectural.com IS a main brand and is now the 6th crawl target with its own DOMAIN/brand/authority rows (this plan); aiSpire's WAC-Group affiliation is intentional but one level below the mains — encoded as the 1.1 tier. `waclighting.com.cn` remains a non-target (no decision requested or made); out of scope unless Davis says otherwise.
- **RESOLVED — Cross-brand bleed (Davis 2026-07-20):** MF content on schonbek's sitemap is skipped, confirmed (was: verify with team).
- **RESOLVED — aiSpire comparison pages (Davis condition + live verification, 2026-07-20):** `/comparison/` + `/abicuscomparison/` stay fully denylisted — 100% of their content is one raster image naming real competitors (Legrand, ENTTEC/"D|NTEC", Engineering Solutions Inc., American Lighting); no partial-ingest option exists; the only path to this material is an out-of-band, manually-authored competitor-free ABiCUS summary (optional follow-on, out of crawl scope). Policy and fixtures encoded in B/I.
- **RESOLVED (v2.1) — category-listing `?p=` truncation (planner-A objection):** the A-section per-host `?p=` exception now covers base category listings, closing the contradiction between the global bare-`p` strip and the facet-drop safety argument / E.2b ID enumeration; guarded by the frontier.test preserved-vs-faceted fixture pair, rollout step 6's sampled-category traversal check, and the dashboard pagination-coverage stat.
- **Public-surface guardrail:** competitor-name suppression confirmed load-bearing for the aiSpire comparison denylist (now grounded in verified page content, not just policy caution).
- **NEW follow-on (logged, out of this plan's scope) — wacarchitectural PDP resolver:** numeric-ID Blazor routes are structurally incompatible with the WIES slug/`?s=` method; needs a name/model-code resolver over both regional catalogs before any pdp_urls/product_documents work for the brand; not an E.0 extension. v1 = evidence harvest + report-only.
- **NEW — `productinfo.ts` `BRAND_SITES` gap (adjacent pre-existing bug, flag to team):** 'WAC Architectural' is a CANONICAL_BRAND but missing from `BRAND_SITES` ⇒ `brandSite()`/`canonicalUrlFor()` silently emits waclighting.com URLs for the brand's products.
- **wacarchitectural project-detail value** — captions likely `too_thin`; held opt-in-off; revisit if the team wants case-study grounding.
- **Confirmed site bugs → report to web team, no plan change:** (1) wacgroup `/commercial/` breadcrumb leaf reads `Architectural` (Davis confirmed real, cosmetic); (2) wacarchitectural `<html lang="zh-CN">` on all-English pages; (3) wacarchitectural `/na/projects` listing hardcodes `/int/project-detail/*` links (the /na detail namespace orphaned from its own listing).
- Bot Management — transparent UA only; Browser-Rendering fallback needs WAC-IT allowlist before waclighting; `bot_block`≠gone. (wacarchitectural exempt: plain nginx, no Cloudflare.)
- Shared WooCommerce admin-page leak (`ppidjump`/`sku-urls`/`quick-importer`) — spot-check waclighting+aispire page-sitemaps before rollout.
- Zendesk overlap — confirm `support.aispire.com` = the org already under `source_system='zendesk'`; if separate → flag out of scope.
- Third asset kind (out of scope, flag) — INST_SHEET install sheets + IES/dimming zips beyond spec_sheet/manual; **wacarchitectural strengthens this: `_INSSHT.pdf` installation sheets, `_SPECS`/`_C` cutsheet variants, and hashed `.pdf`/`.zip` bundles confirmed live on its PDPs (no filename convention — anchor-text/DOM classification required there)**; decide fold-into-`manual` vs new doc_type before E.4 handles it.
- News staleness — 0.9 + superseded-on-404; decide age cap.
- Repo public — keep plan/PRs/fixtures/commits free of crawled copy, financials, customer data (incl. no competitor-comparison image content in fixtures).

---

## Addendum (2026-07-20, post-ratification — Davis decision + coverage verification)

**WAC Architectural catalog status (RESOLVED by Davis).** WAC Architectural products are NOT in the Sales Layer feed yet — **they are going to be added.** This closes the E.2b open question. Consequences, in order:

1. **Nothing in this plan blocks on the catalog — and the brand gets INTERIM product knowledge now (Davis, 2026-07-20).** v1 scope for wacarchitectural.com is UPGRADED from evidence-harvest-only: enable `web_product` ingestion for THIS BRAND ONLY (the off-by-default rule stands for the other five, whose product facts live in the catalog; this brand has no catalog rows, so PDP prose is the only source). Each `/na/product-detail/{id}` PDP becomes a `kb_documents` row (`doc_type='web_product'`, brand='WAC Architectural', scope public, authority 0.8) and is chunked/embedded normally; crawl-discovered spec-sheet PDFs are ingested as spec-sheet kb docs WITHOUT `product_documents` rows (kb_documents needs no SKU; product_documents is the SKU join and waits for the catalog). Evidence (model codes, spec links) still lands in `crawl_frontier`. Thom can answer WAC Architectural product questions immediately, citing live PDPs. **Constraint verified in code:** crawl-sourced rows must NEVER be staged into `products` — the daily Sales Layer sync hard-prunes rows absent from the feed (`apps/api/src/saleslayer.ts` `delete().lt("synced_at", syncedAt)`), so interim product knowledge lives exclusively in the kb store. What the interim cannot provide (inherently SKU-keyed): product cards, `product_semantic_search`, `pdp_urls`/`product_documents` rows.
2. **When the products land in Sales Layer:** a new raw brand code must be mapped in `BRAND_NORMALIZATION` (→ "WAC Architectural") and a `DOMAIN` entry added (`www.wacarchitectural.com` — note www canonical host), in the shared taxonomy module per E.0. The products then flow into `products` via the existing sync, get embeddings via docs-ingest step A2 automatically.
3. **Then the follow-on resolver** (name/model-code match over the harvested frontier evidence — NOT the WIES slug/`?s=` method, which is structurally incompatible with numeric-ID Blazor routes) links PDPs → SKUs and fills `pdp_urls`/`product_documents` for the brand. The evidence collected in v1 means this is a matching job, not a re-crawl. At cutover, decide whether the interim `web_product` prose docs stay (marketing copy alongside catalog/spec data) or are flipped to `superseded`.

**Coverage verification (live DB, 2026-07-20).** Current per-brand linking state the plan's E-section heals: WAC Lighting 2,099/2,110 PDP URLs + 1,796/2,110 spec sheets; Modern Forms 569/573 + 439/573; AiSpire 479/481 + 463/481; **Schonbek 0/1,228 PDP URLs** (spec sheets 100% via PHP template) vs 1,240 sitemap PDPs — the headline reconciliation target. `product_documents`: 4,338/4,390 SKUs have ≥1 doc; 0 family-level rows yet. Residual after this plan: the Schonbek bare-title tail (goes to review report, not auto-linked) and WAC Architectural (pending catalog, above).

## Addendum 2 (2026-07-20 — regional product availability, per Davis)

**The facts.** (1) wacarchitectural.com has TWO product regions with distinct availability: `/na` = Domestic — available ONLY in North America and the Caribbean; `/int` = International — available in the rest of the world EXCEPT China. These are different product sets, not mirrored content. (2) schonbek.com sells international products through the regular menu at `/international/` (category pages `/international/{bath,chandelier,flush-mount-semi-flush-mount,mini-pendant,pendant,recessed-light,wall-sconce}` + `/international/collections/?family=<name>` facets). Live-verified: international category product links use the legacy `/products/?product=<slug>/` shape; the underlying PDPs are normal `/product/<slug>/` pages that are ALREADY in the product sitemaps (e.g. `arlington-12` — export models carry an "E" suffix, `1302E`, 220V, with PPID present), and they render a geo-gated "not yet available in your area" banner to a North-American client IP.

**Plan deltas.**
1. **wacarchitectural `/int` dedup is CONTENT-PAGES-ONLY — never product spaces.** The ratified per-site bullet's "prose-level /int dedup" applies to about/news/company pages. Product listings and `/int/product-detail/{id}` PDPs are a distinct regional catalog and are crawled, harvested, and (per Addendum 1) ingested as `web_product` in their own right.
2. **Region availability is stamped into the interim web_product docs.** The chunk header (section C's `"{brand} — {title}…"` shape) gains an availability line derived from the URL region: `/na/...` → "Availability: North America and the Caribbean only"; `/int/...` → "Availability: international (rest of world), not available in China." This is text-level — no schema change — and is what lets Thom answer "can I get this in Europe?" correctly. `crawl_frontier` PDP evidence records the region (derivable from the stored URL; no new column required, though `region text` MAY be added to 0054 for query convenience).
3. **The follow-on wacarchitectural resolver must treat region as an attribute, not a duplicate-key.** The same product appearing in both regional catalogs (if that occurs) is one logical product with two availability regions — match on model code, union the regions; never discard one region's PDP as a dup of the other.
4. **Schonbek taxonomy additions:** `/international/` landing + `/international/<category>` → `web_category`, skip (same as other category pages); `/international/collections/?family=` → facet junk (the existing `?family` strip covers it — verify it is in the strip list). NO new PDP frontier source: international PDPs are already in `product-sitemap*.xml`. Reconciliation note: export "E" model codes (e.g. `1302E`) may resolve to variant-level or not-in-feed products — expected `unresolved` entries in the report, now explainable as international-only items; do NOT force-match an E-code to its non-E domestic sibling.
5. **Geo-gating caution (Schonbek):** the "not yet available in your area" banner is client-IP-dependent junk text — irrelevant while Schonbek PDP prose stays un-chunked (off by default), but it MUST be stripped if `web_product` is ever enabled for Schonbek, and it does not affect evidence harvest (PPID/model code remain in the HTML).
6. **China note:** neither region's availability includes China (waclighting.com.cn serves that market separately, outside this plan's roster).

## Addendum 3 (2026-07-20 — WAC Architectural regional specs are DISTINCT, per Davis)

**The fact.** For WAC Architectural products, specifications AND spec sheets are TOTALLY DIFFERENT between NA (Domestic) and INT (International). NA and INT are not one product with two availability regions — they are distinct spec entities that may share a marketing-level product identity.

**This SUPERSEDES Addendum 2's resolver rule ("match on model code, union the regions") for this brand. Corrected model:**
1. **Interim kb ingestion: one `web_product` doc per (product, region) — never merged, never deduped across regions.** `/na/product-detail/{id}` and `/int/product-detail/{id}` each get their own `kb_documents` row even when they describe the "same" product. `external_id` = the full canonical URL (already region-distinct), and the chunk-header region line (Addendum 2 §2) is what keeps retrieved spec facts region-attributed. Cross-region near-dup detection must be DISABLED for this brand's product spaces (a near-identical prose shell with different spec tables is exactly the case a text-similarity dedup would wrongly collapse).
2. **Spec-sheet kb docs are region-tagged and kept separate.** An NA spec sheet and an INT spec sheet for the same family are two `kb_documents` rows (distinct URLs/external_ids). Title/label must carry the region — "Specification Sheet (NA)" / "Specification Sheet (International)" — so citations are unambiguous.
3. **Retrieval behavior:** Thom must never present INT specs (voltage, certifications, photometrics) as answers to NA questions or vice versa. With region stamped in every chunk header, the model can and should qualify spec answers by region; add a probe pair to the rollout verification (same product family asked "in the US" vs "in Europe" → region-correct spec sheet cited).
4. **Cutover mapping (when Sales Layer lands):** expect NA and INT to arrive as distinct SKUs/material numbers; the resolver matches each regional PDP to its regional SKU. If instead a single SKU spans regions, `product_documents` (a join table) holds BOTH regional spec sheets as separate labeled rows, and which one feeds `pdp_urls.spec_sheet_url` (a single column) is an explicit flagged decision — do not silently pick one.
5. **Availability (Addendum 2) still stands** — /na = NA+Caribbean only, /int = rest of world except China — it is the spec/doc identity that must not be unioned, not the availability facts.
